# Lessons

- The imported reference script depended on data files outside this repository and on several astronomy packages that were not installed in the current environment. The first runnable demo should keep the boundary-driven tiling separate from richer background-map preprocessing.
